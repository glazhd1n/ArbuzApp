import SwiftUI

struct ProfileScreen: View {
    var body: some View {
        VStack {
            Text("Профиль пользователя")
                .font(.largeTitle)
        }
        .navigationTitle("Профиль")
    }
}
